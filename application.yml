server:
  port: 8088

spring:
  datasource:
    username: root
    password: ${DB_PASSWORD:}
    url: jdbc:mysql://localhost:3306/ant-live?characterEncoding=utf-8&serverTimezone=GMT%2b8
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      max-wait: 60000
      initial-size: 5
      min-idle: 5
      max-active: 20

  mail:
    host: smtp.qq.com
    username: 2191684957@qq.com
    password: ${MAIL_PASSWORD:}
    default-encoding: UTF-8
    port: 465
    protocol: smtps
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
            protocols: TLSv1.2
            trust: smtp.qq.com
          socketFactory:
            class: javax.net.ssl.SSLSocketFactory
            fallback: false
          connectiontimeout: 5000
          timeout: 60000
          writetimeout: 60000
        smtps:
          auth: true
          ssl:
            enable: true
            protocols: TLSv1.2
            trust: smtp.qq.com
          connectiontimeout: 5000
          timeout: 60000
          writetimeout: 60000

  redis:
    database: 0
    host: localhost
    port: 6379
    password: 
    jedis:
      pool:
        max-active: 8
        max-idle: 8
    timeout: 5000

  servlet:
    multipart:
      enabled: true
      max-file-size: 4096MB
      max-request-size: 4096MB

tencent:
  sms:
    appid: 000
    appKey: 000
    templateId: 000
    templateNoticeId: 000
    smsSign: 000
  live:
    domain: live.imhtb.cn
    appName: live
    secretId: 000
    secretKey: 000
  cos:
    bucketName: 000
    path: https://xxx.cos.ap-chengdu.myqcloud.com/

aliyun:
  sms:
    enabled: true
    accessKeyId: ${ALIYUN_SMS_ACCESS_KEY_ID:}
    accessKeySecret: ${ALIYUN_SMS_ACCESS_KEY_SECRET:}
    endpoint: dypnsapi.aliyuncs.com
    signName: "速通互联验证码"
    templateCode: "100001"
    templateParam: '{"code":"##code##","min":"5"}'
    countryCode: ""
    schemeName: ""
    smsUpExtendCode: ""
    codeLength: 6
    codeType: 1
    validTimeSeconds: 300
    duplicatePolicy: 1
    intervalSeconds: 10
    returnVerifyCode: true

mybatis-plus:
  global-config:
    banner: false
    db-config:
      id-type: auto
      logic-not-delete-value: 0
      logic-delete-value: 1

alipay:
  enabled: true
  verifySign: false
  gatewayUrl: https://openapi-sandbox.dl.alipaydev.com/gateway.do
  appId: "9021000163606120"
  appPrivateKey: ${ALIPAY_APP_PRIVATE_KEY:}
  alipayPublicKey: ${ALIPAY_PUBLIC_KEY:}
  notifyUrl: "http://localhost:8088/api/v1/wallet/alipay/notify"
  returnUrl: http://localhost:5173/#/center/dollar/wallet
  syncReturnUrl: "http://localhost:8088/api/v1/wallet/alipay/return"
  charset: UTF-8
  format: json
  signType: RSA2
  productCode: FAST_INSTANT_TRADE_PAY
  subjectPrefix: "PulseLive 开心果充值"

minio:
  endpoint: http://localhost:9000
  ip: localhost
  port: 9000
  accessKey: minioadmin
  secretKey: minioadmin
  bucketName: live.file.bucket
  relative: true

guard:
  enabled: true
  endpoint: http://localhost:8300/check
  intervalSeconds: 2

dromara:
  x-file-storage:
    default-platform: minio-1 # 默认使用的存储平台
    thumbnail-suffix: ".min.jpg" # 缩略图后缀
    minio:
      - platform: minio-1
        enable-storage: true
        access-key: minioadmin
        secret-key: minioadmin
        end-point: http://localhost:9000
        bucket-name: live.file.bucket
        domain: http://localhost:9000/live.file.bucket/ # 访问域名，注意“/”结尾，例如：http://minio.abc.com/abc/
        base-path:  # 基础路径 xx/
